# otus_lab9
```mkdir ./build && cd ./build```  
```cmake ../```  
```make```  
```sudo make install```  
  
В проекте:  
```find_package(async 0.1 REQUIRED)```  
```target_link_libraries(${PROJECT_NAME} PUBLIC async)```

